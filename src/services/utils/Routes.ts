import baseApiUrl from "../BaseApiUrl";

export const Routes = {
    ROLES : {
        INDEX: baseApiUrl + '/api/roles',
        STORE: baseApiUrl + '/api/rol',
        UPDATE: baseApiUrl + '/api/rol',
        SHOW: baseApiUrl + '/api/rol',
        DELETE: baseApiUrl + '/api/rol',
    }
}